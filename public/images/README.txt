Letakkan aset gambar landing page di folder ini:

1. logo-uml.png
   Logo Universitas Muhammadiyah Lampung (disarankan PNG transparan, ~200x200px).
   Tampil di navbar & section "Tentang". Jika tidak ada, otomatis tampil fallback teks "UML".

2. slider/1.jpg, slider/2.jpg, slider/3.jpg
   Gambar background kampus untuk hero slider (disarankan FULL HD landscape, mis. 1920x1080px).
   Jika tidak ada, hero tetap tampil dengan latar gradien hijau (tanpa error).
   (Atau atur slider lewat menu admin "Slider" — itu yang diprioritaskan.)

3. rektor.png
   Foto REKTOR berupa PNG TRANSPARAN (cutout, background sudah dihapus).
   Tampil menyatu di sisi kanan hero dengan gradient mask (pinggir memudar) + parallax.
   Disarankan potret tinggi, mis. 800x1100px, latar transparan.
   Jika tidak ada, sisi kanan hero otomatis kosong (teks tetap rapi).

Setelah menaruh file, tidak perlu build ulang — cukup refresh halaman.
